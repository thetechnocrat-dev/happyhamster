import './globals.css'
import type { Metadata } from 'next'
import { Inter } from 'next/font/google'

const inter = Inter({ subsets: ['latin'] })

export const metadata: Metadata = {
  title: 'Happy Hamster',
  description: 'Unlocking the potential of pet hamsters for public healthcare and humane research. Happy Hamster leverages decentralized tech and the vibrant Nouns community to transform pet care and biomedicine.',
}

export default function RootLayout({
  children,
}: {
  children: React.ReactNode
}) {
  return (
    <html lang="en">
      <body className={inter.className}>{children}</body>
    </html>
  )
}
